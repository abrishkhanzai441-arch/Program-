def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    # Handle division by zero
    if b == 0:
        return "Error: Cannot divide by zero"
    return a / b

def main():
    while True:  # Loop keeps the calculator running until user exits
        print("\n----- Menu-Driven Calculator -----")
        print("1. Addition (+)")
        print("2. Subtraction (-)")
        print("3. Multiplication (×)")
        print("4. Division (÷)")
        print("5. Exit")

        choice = input("Choose an operation (1-5): ")

        # Exit option handled first so we don't ask for numbers unnecessarily
        if choice == "5":
            print("Exiting calculator. Goodbye!")
            break

        # Validate menu choice before asking for numbers
        if choice not in ("1", "2", "3", "4"):
            print("Invalid choice. Please select a number between 1 and 5.")
            continue

        # Get the two numbers to operate on
        try:
            num1 = float(input("Enter the first number: "))
            num2 = float(input("Enter the second number: "))
        except ValueError:
            print("Invalid input. Please enter numeric values.")
            continue

        # Perform the chosen operation
        if choice == "1":
            result = add(num1, num2)
            print(f"Result: {num1} + {num2} = {result}")
        elif choice == "2":
            result = subtract(num1, num2)
            print(f"Result: {num1} - {num2} = {result}")
        elif choice == "3":
            result = multiply(num1, num2)
            print(f"Result: {num1} × {num2} = {result}")
        elif choice == "4":
            result = divide(num1, num2)
            print(f"Result: {num1} ÷ {num2} = {result}")

if __name__ == "__main__":
    main()